# 🤲 Студия керамики «Майолика» — Telegram Mini App

Приложение для записи на мастер-классы по лепке из глины.

## 📱 Функции

### Mini App (для клиентов)
- Запись на мастер-класс с указанием имени, фамилии, возраста и пола ребёнка
- Выбор времени начала (10:00, 11:00, 12:00, 13:00)
- Выбор количества часов (1-4)
- Автоматический расчёт стоимости (700₽/час)
- Отправка подтверждения в Telegram после записи

### Записная книжка (админ-панель)
- Защита паролем: `2252525`
- Таблица всех записей с фильтрацией по дате
- Статистика: количество записей, выручка
- Визуальное расписание по времени
- Удаление записей
- Автообновление каждые 30 секунд

### 📢 Рассылка сообщений (NEW!)
- Отправка новостей и акций всем подписчикам бота
- Поддержка текста и изображений
- Список подписчиков с датой подписки
- Предпросмотр сообщения перед отправкой

### Уведомления в Telegram
- Админ получает сообщение о каждой новой записи
- Клиент получает подтверждение с деталями записи

## 🔗 URL-адреса

| Страница | URL |
|----------|-----|
| Mini App | `https://ваш-домен.vercel.app/` |
| Админ-панель | `https://ваш-домен.vercel.app/?page=admin` |

## 🚀 Деплой на Vercel

### 1. Настройка Upstash Redis

1. Зайдите на [upstash.com](https://upstash.com)
2. Создайте аккаунт
3. Нажмите **Create Database**
4. Выберите регион и тип **Regional**
5. Скопируйте:
   - `UPSTASH_REDIS_REST_URL`
   - `UPSTASH_REDIS_REST_TOKEN`

### 2. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Придумайте имя и username бота
4. Скопируйте **токен бота**

### 3. Получение Chat ID для уведомлений

1. Напишите что-нибудь вашему боту
2. Откройте: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
3. Найдите `"chat":{"id":XXXXXXXX}` - это ваш Chat ID
4. Или добавьте бота [@userinfobot](https://t.me/userinfobot) и узнайте свой ID

### 4. Деплой

1. Загрузите код на GitHub
2. На [vercel.com](https://vercel.com) подключите репозиторий
3. **Добавьте переменные окружения:**

| Переменная | Значение |
|------------|----------|
| `UPSTASH_REDIS_REST_URL` | URL от Upstash |
| `UPSTASH_REDIS_REST_TOKEN` | Токен от Upstash |
| `TELEGRAM_BOT_TOKEN` | Токен от BotFather |
| `TELEGRAM_ADMIN_IDS` | ID админов через запятую: `917022431,706357294` |

4. Нажмите **Deploy**

### 5. Настройка Webhook для бота (для рассылки)

После деплоя нужно подключить webhook, чтобы бот запоминал подписчиков:

1. Откройте в браузере:
```
https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook?url=https://ваш-проект.vercel.app/api/telegram-webhook
```

2. Вы должны увидеть: `{"ok":true,"result":true,"description":"Webhook was set"}`

### 6. Подключение Mini App к боту

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots` → выберите бота
3. **Bot Settings** → **Menu Button** → **Configure Menu Button**
4. Отправьте URL: `https://ваш-проект.vercel.app`
5. Введите текст кнопки: `Записаться`

## 💡 Как работает рассылка

1. Когда пользователь пишет `/start` боту — он добавляется в список подписчиков
2. В админ-панели нажмите кнопку **📢 Рассылка**
3. Напишите текст сообщения (можно добавить изображение)
4. Нажмите **Отправить** — сообщение уйдёт всем подписчикам

## 📞 Команды бота

После настройки webhook бот отвечает на команды:

- `/start` — Приветствие и информация о мастерской
- `/help` — Список команд
- `/info` — О мастерской
- `/price` — Цены

## 💡 Настройка

### Изменить информацию о мастерской

Отредактируйте файл `src/types/booking.ts`:

```typescript
export const WORKSHOP_INFO = {
  name: 'Название вашей студии',
  address: 'Ваш адрес',
  phone: 'Ваш телефон',
};
```

### Изменить цену

В том же файле:

```typescript
export const PRICE_PER_HOUR = 700; // рублей за час
```

### Изменить пароль админки

Отредактируйте файл `src/components/AdminPanel.tsx`:

```typescript
const ADMIN_PASSWORD = 'ваш_новый_пароль';
```

## 📞 Поддержка

Если возникли вопросы — создайте Issue в репозитории.
